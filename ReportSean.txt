Section III (update function)

The update function prints a numbered list of all contacts and prompts the user to select one. Then, the selected contact is deleted and adduser.sh is called to create a new user in its place.
